# termhelp

`termhelp` is a local-first CLI tutor for terminal beginners.

Current scope: package setup + CLI entrypoint only.

## Quick start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -v .[dev]
termhelp --help
pytest
